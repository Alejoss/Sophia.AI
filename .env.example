# Docker Compose Environment Variables
# This file is used by docker-compose.prod.yml for variable substitution
# Copy this to .env in the project root (same directory as docker-compose.prod.yml)
#
# These variables are used ONLY for Docker Compose variable substitution.
# The actual values are loaded from acbc_app/.env via env_file in docker-compose.prod.yml
#
# You can either:
# 1. Set these here (recommended for Docker Compose substitution)
# 2. Export them in your shell before running docker compose
# 3. Leave them empty and ensure acbc_app/.env has POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD

# Database configuration (for Docker Compose variable substitution)
# These should match the values in acbc_app/.env
DB_NAME=academiablockchain_prod
DB_USER=postgres
DB_PASSWORD=your-secure-password-here

# Alternative: Use POSTGRES_* variables (preferred)
# POSTGRES_DB=academiablockchain_prod
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-secure-password-here
